import { mockUser } from '../services/test-utils';

export const mockReqHeader = {
  'Content-Type': 'application/json',
  mockUser
}

export const mockUrls = {
  'transferPlayerPlayback': 'http://mock.com/playdevice/:deviceid/song'
};